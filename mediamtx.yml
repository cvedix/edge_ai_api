# Allow publishers by default for all paths
pathDefaults:
  source: publisher

paths:
  # explicit paths used by the test flows
  live/stream1:
    source: publisher
  live/jam_test:
    source: publisher
  live/jam_test_0:
    source: publisher
